apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: "ots3"
  annotations:
    grafana/dashboard-selector: "tags @> 'vth'"
    grafana/overview-dashboard: 'http://localhost:3000/d/gitlab_ci_jobs/gitlab-ci-jobs?orgId=1&kiosk'
spec:
  type: service
  owner: user:guest
  lifecycle: experimental
